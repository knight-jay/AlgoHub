# 管理员模块接口测试记录

**测试执行人：** 尹冰洁
**测试时间：** 2026-05-05
**测试范围：** 管理员模块 3 个核心功能：获取用户列表、禁用/启用账号、修改用户角色

## 一、 接口功能测试执行清单

### 1. 获取用户列表功能
| 编号 | 接口路径及方法 | 测试场景描述 | 模拟输入 (Header/参数) | 预期结果 (据代码设计) | 实际结果 | 状态 |
|---|---|---|---|---|---|---|
| TC-A01 | `GET /api/admin/users` | 管理员正常获取列表 | Header 携带 Admin Token | 返回全量用户列表，且密码均被过滤为 null | 成功返回列表，password 为 null | ✅ 通过 |
| TC-A02 | `GET /api/admin/users` | **[越权]** 学生身份尝试获取 | Header 携带 Student Token | code: 403, "无权限，仅管理员可操作" | 正常拦截返回 403 | ✅ 通过 |
| TC-A03 | `GET /api/admin/users` | 未登录获取 | 不携带 Token | 拦截器返回未登录 | 正常拦截 | ✅ 通过 |

### 2. 禁用/启用账号功能
| 编号 | 接口路径及方法 | 测试场景描述 | 模拟输入 (参数/Body) | 预期结果 | 实际结果 | 状态 |
|---|---|---|---|---|---|---|
| TC-A04 | `PUT /api/admin/users/1/status` | 管理员禁用账号 | `{"locked": 1}` | code: 200, "账号已禁用" | "账号已禁用" | ✅ 通过 |
| TC-A05 | `PUT /api/admin/users/1/status` | 管理员启用账号 | `{"locked": 0}` | code: 200, "账号已启用" | "账号已启用" | ✅ 通过 |
| TC-A06 | `PUT /api/admin/users/999/status` | 操作不存在的用户ID | `{"locked": 1}`，URL填999 | code: 500, "用户不存在" | "用户不存在" | ✅ 通过 |
| TC-A07 | `PUT /api/admin/users/1/status` | **[异常]** 传入非法数字 | `{"locked": 999}` | code: 500, "参数错误：locked 必须为 0 或 1" | 正常拦截提示 | ✅ 通过 |
| TC-A08 | `PUT /api/admin/users/1/status` | **[异常]** 参数为空 | `{}` (不传locked) | code: 500, "参数错误：locked 必须为 0 或 1" | 正常拦截提示 | ✅ 通过 |
| TC-A09 | `PUT /api/admin/users/1/status` | **[越权]** 学生身份尝试禁用 | Header 带 Student Token | code: 403, "无权限，仅管理员可操作" | 正常拦截返回 403 | ✅ 通过 |

### 3. 修改用户角色功能
| 编号 | 接口路径及方法 | 测试场景描述 | 模拟输入 (参数/Body) | 预期结果 | 实际结果 | 状态 |
|---|---|---|---|---|---|---|
| TC-A10 | `PUT /api/admin/users/2/role` | 正常修改为管理员 | `{"role": "ADMIN"}` | code: 200, "角色修改成功" | "角色修改成功" | ✅ 通过 |
| TC-A11 | `PUT /api/admin/users/2/role` | 小写传参测试边界 | `{"role": "student"}` | 后端支持转换，提示修改成功 | "角色修改成功" | ✅ 通过 |
| TC-A12 | `PUT /api/admin/users/999/role` | 操作不存在的用户ID | `{"role": "ADMIN"}`，URL填999 | code: 500, "用户不存在" | "用户不存在" | ✅ 通过 |
| TC-A13 | `PUT /api/admin/users/2/role` | **[异常]** 传入非法角色名 | `{"role": "TEACHER"}` | code: 500, "参数错误：role 必须为 STUDENT 或 ADMIN" | 正常拦截提示 | ✅ 通过 |
| TC-A14 | `PUT /api/admin/users/2/role` | **[越权]** 学生身份尝试改权 | Header 带 Student Token | code: 403, "无权限，仅管理员可操作" | 正常拦截返回 403 | ✅ 通过 |
