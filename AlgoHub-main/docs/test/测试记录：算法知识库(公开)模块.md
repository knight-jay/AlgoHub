# 算法知识库模块接口测试记录

**测试执行人：** 尹冰洁
**测试时间：** 2026-05-05
**测试范围：** 算法知识库（公开）模块的 6 个接口功能

## 一、 算法模块接口功能测试执行清单

### 1. 获取算法分类树功能
| 编号 | 接口路径及方法 | 测试场景描述 | 模拟输入 (参数/Path) | 预期结果 (据代码设计) | 实际结果 | 状态 |
|---|---|---|---|---|---|---|
| TC-A01 | `GET /api/algorithm/categories` | 正常获取完整顶层分类树 | 无 | 返回 code: 200，data 为分类树列表 | code: 200，数据正常 | ✅ 通过 |

### 2. 获取子分类功能
| 编号 | 接口路径及方法 | 测试场景描述 | 模拟输入 (参数/Path) | 预期结果 (据代码设计) | 实际结果 | 状态 |
|---|---|---|---|---|---|---|
| TC-A02 | `GET /api/algorithm/categories/{parentId}/children` | 获取某有效分类的子分类 | Path: `parentId = 1` | 返回 code: 200，对应子分类列表 | code: 200 | ✅ 通过 |
| TC-A03 | `GET /api/algorithm/categories/{parentId}/children` | 获取不存在分类的子分类 | Path: `parentId = 9999` | 返回 code: 200，data 为空数组 [] | 返回空数组 | ✅ 通过 |

### 3. 搜索算法功能
| 编号 | 接口路径及方法 | 测试场景描述 | 模拟输入 (Query 参数) | 预期结果 | 实际结果 | 状态 |
|---|---|---|---|---|---|---|
| TC-A04 | `GET /api/algorithm/search` | 正常关键字分页搜索 | `keyword=排序`, `page=1`, `pageSize=10` | 返回分页数据 PageResult，包含匹配记录 | 分页数据正确 | ✅ 通过 |
| TC-A05 | `GET /api/algorithm/search` | 测试默认分页参数生效 | 只传 `keyword=树`，不传 page 和 pageSize | 自动应用 page=1, pageSize=10 返回数据 | 默认分页生效 | ✅ 通过 |
| TC-A06 | `GET /api/algorithm/search` | 搜索不存在的关键字 | `keyword=阿巴阿巴` | 返回 code: 200，分页 records 为空列表 | 列表为空 | ✅ 通过 |
| TC-A13 | `GET /api/algorithm/search` | 搜索关键字传入纯空格 | `keyword=   `（纯空格，无其他文字） | 拦截纯空格关键字，提示“搜索关键字不能为空” | 未拦截，直接返回全表数据，存在性能隐患 | ❌ 未通过（发现Bug） |
| TC-A14 | `GET /api/algorithm/search` | 分页参数传入超大pageSize值 | `keyword=树`, `page=1`, `pageSize=100000` | 限制单次最大查询条数（如不超过100条），防止批量拉取 | 无任何拦截，接口正常返回数据，存在性能与OOM隐患 | ✅ 通过（❌但存在优化空间） |

### 4. 获取算法详情功能
| 编号 | 接口路径及方法 | 测试场景描述 | 模拟输入 (参数/Path) | 预期结果 (据代码设计) | 实际结果 | 状态 |
|---|---|---|---|---|---|---|
| TC-A07 | `GET /api/algorithm/{id}` | 查询存在的算法详情 | Path: `id = 1` | 返回 code: 200，包含算法详细字段 | code: 200，返回实体数据 | ✅ 通过 |
| TC-A08 | `GET /api/algorithm/{id}` | 查询不存在的算法详情 | Path: `id = 99999` | 触发后端校验，返回错误信息 "算法不存在" | 提示"算法不存在" | ✅ 通过（校验正常） |
| TC-A09 | `GET /api/algorithm/{id}` | 传入非法类型 ID | Path: `id = abc` | 返回 code:500，提示 "服务器内部错误" | 报错异常拦截 | ✅ 通过 |

### 5. 按分类浏览功能
| 编号 | 接口路径及方法 | 测试场景描述 | 模拟输入 (参数) | 预期结果 | 实际结果 | 状态 |
|---|---|---|---|---|---|---|
| TC-A10 | `GET /api/algorithm/category/{categoryId}` | 根据分类ID查算法 | Path: `categoryId=1` <br> Query: `page=1`, `pageSize=5` | 返回该分类下的算法分页列表 | 数据匹配该分类 | ✅ 通过 |
| TC-A15 | `GET /api/algorithm/category/{categoryId}` | 查询不存在的分类ID | Path: categoryId=99999 <br> Query: page=1, pageSize=5 | 提示“该分类不存在或已删除” | 返回code:200空列表，无任何文字提示 | ✅ 通过（❌但存在优化空间） |

### 6. 按难度筛选功能
| 编号 | 接口路径及方法 | 测试场景描述 | 模拟输入 (参数) | 预期结果 | 实际结果 | 状态 |
|---|---|---|---|---|---|---|
| TC-A11 | `GET /api/algorithm/difficulty/{difficulty}` | 根据难度查算法 (简单) | Path: `difficulty=EASY` <br> Query: `page=1` | 返回难度为 EASY 的算法列表 | 数据全为简单题 | ✅ 通过 |
| TC-A12 | `GET /api/algorithm/difficulty/{difficulty}` | 根据难度查算法 (困难) | Path: `difficulty=HARD` <br> Query: `page=1` | 返回难度为 HARD 的算法列表 | 数据全为困难题 | ✅ 通过 |

