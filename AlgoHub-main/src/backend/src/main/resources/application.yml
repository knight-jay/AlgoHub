server:
  servlet:
    session:
      persistent: false

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/algohub?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: zxj123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

jwt:
  secret: AlgoHub2026SecretKeyForJwtHS256Algo!!!!